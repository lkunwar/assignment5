<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Register - Climate Solutions</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <main class="p-10 max-w-lg mx-auto">
    <h1 class="text-2xl font-bold mb-6">Register</h1>
    <% if (successMessage) { %>
      <div class="alert alert-success mb-4 p-4 bg-green-100 text-green-700 rounded">
        <span><%= successMessage %></span>
      </div>
      <a href="/login" class="btn btn-success w-full bg-green-600 text-white py-2 rounded">Proceed to Log in</a>
    <% } else { %>
      <form method="POST" action="/register" class="space-y-4">
        <input class="input input-bordered w-full" type="text" name="userName" placeholder="User Name" value="<%= userName %>" required>
        <input class="input input-bordered w-full" type="password" name="password" placeholder="Password" required>
        <input class="input input-bordered w-full" type="password" name="password2" placeholder="Confirm Password" required>
        <input class="input input-bordered w-full" type="email" name="email" placeholder="Email Address" required>
        <button class="btn btn-primary w-full bg-blue-600 text-white py-2 rounded" type="submit">Register</button>
      </form>
      <% if (errorMessage) { %>
        <div class="alert alert-error mt-4 p-4 bg-red-100 text-red-700 rounded">
          <span><%= errorMessage %></span>
        </div>
      <% } %>
    <% } %>
  </main>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= page %> - Climate Solutions</title>
  <link rel="stylesheet" href="/css/tailwind.css">
  <link rel="stylesheet" href="/css/main.css">
</head>
<body class="bg-gray-50 text-gray-900 min-h-screen">
  <nav class="bg-white shadow mb-6">
    <div class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
      <a href="/" class="text-xl font-bold text-blue-700">Climate Solutions</a>
      <div class="space-x-4">
        <a href="/about" class="<%= page === '/about' ? 'text-blue-500 font-semibold' : '' %>">About</a>
        <a href="/solutions/projects" class="<%= page === '/solutions/projects' ? 'text-blue-500 font-semibold' : '' %>">Projects</a>
        <% if (session && session.user) { %>
          <a href="/solutions/addProject" class="<%= page === '/solutions/addProject' ? 'text-blue-500 font-semibold' : '' %>">Add Project</a>
          <a href="/userHistory" class="<%= page === '/userHistory' ? 'text-blue-500 font-semibold' : '' %>">User History</a>
          <a href="/logout" class="text-red-600">Logout</a>
        <% } else { %>
          <a href="/login" class="<%= page === '/login' ? 'text-blue-500 font-semibold' : '' %>">Login</a>
          <a href="/register" class="<%= page === '/register' ? 'text-blue-500 font-semibold' : '' %>">Register</a>
        <% } %>
      </div>
    </div>
  </nav>
  <main class="max-w-6xl mx-auto px-4">

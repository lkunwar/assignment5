<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login - Climate Solutions</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <main class="p-10 max-w-lg mx-auto">
    <h1 class="text-2xl font-bold mb-6">Login</h1>
    <form method="POST" action="/login" class="space-y-4">
      <input class="input input-bordered w-full" type="text" name="userName" placeholder="User Name" value="<%= userName %>" required>
      <input class="input input-bordered w-full" type="password" name="password" placeholder="Password" required>
      <button class="btn btn-primary w-full" type="submit">Login</button>
    </form>
    <% if (errorMessage) { %>
      <div class="alert alert-error mt-4 p-4 bg-red-100 text-red-700 rounded">
        <span><%= errorMessage %></span>
      </div>
    <% } %>
  </main>
</body>
</html>
